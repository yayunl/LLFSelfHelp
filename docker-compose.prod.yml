version: "3.3"

services:

  nginx:
    image: docker.pkg.github.com/yayunl/llfselfhelp/nginx:latest
    volumes:
      - staticfiles:/app/staticfiles
      - mediafiles:/app/mediafiles
    ports:
      - 80:80
      - 5555:5555
    depends_on:
      - web
      - flower

  web:
    image: docker.pkg.github.com/yayunl/llfselfhelp/llfadmin:0.2
    command: /start
#    command: tail -f /dev/null
    ports:
      - 8000:8000
    env_file:
      - env/.env.prod
    depends_on:
      - redis
      - db
    volumes:
      - staticfiles:/app/staticfiles
      - mediafiles:/app/mediafiles

  worker:
    image: docker.pkg.github.com/yayunl/llfselfhelp/llfadmin:0.2
    command: /start-celeryworker
#    command: tail -f /dev/null
    env_file:
      - env/.env.prod
    depends_on:
      - redis
      - db
    volumes:
      - staticfiles:/app/staticfiles
      - mediafiles:/app/mediafiles


  beat:
    image: docker.pkg.github.com/yayunl/llfselfhelp/llfadmin:0.2
    command: /start-celerybeat
    env_file:
      - env/.env.prod
    depends_on:
      - redis
      - db
    volumes:
      - staticfiles:/app/staticfiles
      - mediafiles:/app/mediafiles


  flower:
    image: docker.pkg.github.com/yayunl/llfselfhelp/llfadmin:0.2
    command: /start-flower
#    command: tail -f /dev/null
    volumes:
      - staticfiles:/app/staticfiles
      - mediafiles:/app/mediafiles
    env_file:
      - env/.env.prod
    depends_on:
      - redis
      - db

  db:
    image: postgres:12.0-alpine
#    ports:
#      - 5433:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=p@ssword
      - POSTGRES_DB=llfdb_prod
    volumes:
      - ./compose/production/django/init_prod.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres_data_prod:/var/lib/postgresql/data/

  redis:
    image: redis:latest


volumes:
  postgres_data_prod:
  staticfiles:
  mediafiles:
